# Private registry configuration - tests private registry fallback
registry=https://npm.internal.company.com/
@my-org:registry=https://npm.my-org.com/
@company:registry=https://registry.company.internal/
always-auth=true
//npm.internal.company.com/:_authToken=${NPM_TOKEN}
//npm.my-org.com/:_authToken=${ORG_TOKEN}

# Fallback behavior test
# When package not found in private registry, should fallback to public npmjs.org
